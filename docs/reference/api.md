# API Reference（接口參考）

本文件是 API 參考層入口；接口細節集中在 `docs/reference/api/`（endpoint / request/response schema / error codes）。

## 定位與原則

- 介面/Schema（拆分後索引）：`docs/reference/api/README.md`
- API 契約索引（入口）：`docs/api-contract.md`
- 規則/流程（權威來源）：`docs/modules/`

## 分工

- `docs/api-contract.md` 作為索引入口；接口細節集中在 `docs/reference/api/`。
- `docs/modules/` 保留「規則與流程」，並在需要時被 API 文檔引用。
